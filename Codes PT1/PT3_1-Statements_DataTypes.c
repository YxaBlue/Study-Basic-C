#include<stdio.h>

int main()
{
    // COMMON DATA TYPES:
    char character;
    int integer;
    float floatNum;
    double doubleNum;
    

    // INCOMPLETE TYPE (To be tackled later):
    // void voidVariable;


    // TYPE SPECIFIER:
    long a;             // integer (large)
    long long b;        // integer (very large)
    long double c;      // floating-point number
    short d;            // integer (small)


    // TYPE MODIFIERS (Only for integers):
    unsigned int e = 35;            // unsigned integer (always positive)
    int f = 35;                     // signed integer (positive)
    int g = -35;                    // signed integer (negative)

    unsigned short h = 10;
    short j = -10;
    short k = -10;

    unsigned long l = 100;
    long m = 100;
    long n = -100;

    unsigned long long o = 1000;
    long long p = 1000;
    long long q = -1000;

    signed char r;
    unsigned char s;


    return 0;
}